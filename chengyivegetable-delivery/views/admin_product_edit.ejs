<!DOCTYPE html>
<html lang="zh-Hant">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>編輯商品 - 誠意鮮蔬管理後台</title>
    <link rel="stylesheet" href="/css/admin-dashboard.css">
    <style>
        .edit-container {
            padding: 20px;
            max-width: 900px;
            margin: 0 auto;
        }
        
        .edit-header {
            background: linear-gradient(135deg, #e67e22, #f39c12);
            color: white;
            padding: 20px;
            border-radius: 12px;
            margin-bottom: 30px;
        }
        
        .form-grid {
            display: grid;
            gap: 30px;
        }
        
        .form-section {
            background: white;
            padding: 25px;
            border-radius: 12px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        
        .section-title {
            font-size: 1.3em;
            color: #2c3e50;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 2px solid #ecf0f1;
        }
        
        .form-group {
            margin-bottom: 20px;
        }
        
        .form-label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
            color: #34495e;
        }
        
        .form-input {
            width: 100%;
            padding: 12px;
            border: 2px solid #ecf0f1;
            border-radius: 8px;
            font-size: 1em;
            transition: border-color 0.3s;
        }
        
        .form-input:focus {
            outline: none;
            border-color: #3498db;
            box-shadow: 0 0 0 3px rgba(52, 152, 219, 0.1);
        }
        
        .checkbox-group {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 15px;
        }
        
        .checkbox-group input[type="checkbox"] {
            width: 20px;
            height: 20px;
        }
        
        .image-upload {
            border: 2px dashed #bdc3c7;
            padding: 30px;
            text-align: center;
            border-radius: 8px;
            transition: border-color 0.3s;
        }
        
        .image-upload:hover {
            border-color: #3498db;
        }
        
        .image-upload input {
            display: none;
        }
        
        .upload-btn {
            background: #3498db;
            color: white;
            padding: 12px 24px;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            font-size: 1em;
            transition: background 0.3s;
        }
        
        .upload-btn:hover {
            background: #2980b9;
        }
        
        .image-preview {
            max-width: 200px;
            max-height: 200px;
            margin-top: 15px;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }
        
        .options-list {
            border: 1px solid #ecf0f1;
            border-radius: 8px;
            padding: 15px;
            margin-top: 15px;
        }
        
        .option-item {
            display: flex;
            gap: 15px;
            align-items: center;
            padding: 10px;
            border-bottom: 1px solid #ecf0f1;
        }
        
        .option-item:last-child {
            border-bottom: none;
        }
        
        .option-item input {
            flex: 1;
            padding: 8px;
            border: 1px solid #ddd;
            border-radius: 4px;
        }
        
        .add-option-btn {
            background: #27ae60;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 6px;
            cursor: pointer;
            margin-top: 10px;
        }
        
        .remove-option-btn {
            background: #e74c3c;
            color: white;
            border: none;
            padding: 5px 10px;
            border-radius: 4px;
            cursor: pointer;
            font-size: 0.9em;
        }
        
        .action-buttons {
            display: flex;
            gap: 15px;
            justify-content: center;
            margin-top: 30px;
            padding-top: 20px;
            border-top: 2px solid #ecf0f1;
        }
        
        .save-btn {
            background: #27ae60;
            color: white;
            border: none;
            padding: 15px 40px;
            border-radius: 8px;
            font-size: 1.1em;
            cursor: pointer;
            transition: background 0.3s;
        }
        
        .save-btn:hover {
            background: #229954;
        }
        
        .cancel-btn {
            background: #7f8c8d;
            color: white;
            border: none;
            padding: 15px 40px;
            border-radius: 8px;
            font-size: 1.1em;
            text-decoration: none;
            transition: background 0.3s;
        }
        
        .cancel-btn:hover {
            background: #6c7b7d;
        }
    </style>
</head>
<body>
    <div class="edit-container">
        <div class="edit-header">
            <h1>🛍️ 編輯商品：<%= product.name %></h1>
            <p>完整編輯商品資訊、選項和圖片</p>
        </div>
        
        <form method="POST" action="/admin/products/<%= product.id %>/update-full" enctype="multipart/form-data">
            <div class="form-grid">
                <!-- 基本資訊 -->
                <div class="form-section">
                    <h2 class="section-title">📝 基本資訊</h2>
                    
                    <div class="form-group">
                        <label class="form-label">商品名稱</label>
                        <input type="text" name="name" class="form-input" value="<%= product.name %>" required>
                    </div>
                    
                    <div class="checkbox-group">
                        <input type="checkbox" name="isPricedItem" id="isPricedItem" 
                               <%= product.is_priced_item ? 'checked' : '' %>>
                        <label for="isPricedItem">計價商品（依重量計價）</label>
                    </div>
                    
                    <div class="form-group">
                        <label class="form-label">固定價格（計價商品請留空）</label>
                        <input type="number" step="0.01" name="price" class="form-input" 
                               value="<%= product.price || '' %>" placeholder="例：80">
                    </div>
                    
                    <div class="form-group">
                        <label class="form-label">單位說明</label>
                        <input type="text" name="unitHint" class="form-input" 
                               value="<%= product.unit_hint || '' %>" placeholder="例：每公斤、每顆、每包">
                    </div>
                </div>
                
                <!-- 商品圖片 -->
                <div class="form-section">
                    <h2 class="section-title">📸 商品圖片</h2>
                    
                    <div class="image-upload">
                        <label for="productImage" class="upload-btn">📷 選擇圖片</label>
                        <input type="file" name="productImage" id="productImage" accept="image/*">
                        <p>支援 JPG、PNG 格式，建議尺寸 400x400px</p>
                        
                        <% if (product.image_url) { %>
                            <img src="<%= product.image_url %>" alt="商品圖片" class="image-preview" id="imagePreview">
                        <% } else { %>
                            <img id="imagePreview" class="image-preview" style="display: none;">
                        <% } %>
                    </div>
                </div>
                
                <!-- 商品選項 -->
                <div class="form-section">
                    <h2 class="section-title">⚙️ 商品選項</h2>
                    <p>為商品添加選項（如：削皮、切片、包裝方式等）</p>
                    
                    <div class="options-list" id="optionsList">
                        <% if (product.options && product.options.length > 0) { %>
                            <% product.options.forEach(function(option, index) { %>
                                <div class="option-item">
                                    <input type="text" name="options[<%= index %>][name]" 
                                           placeholder="選項名稱" value="<%= option.name %>" required>
                                    <input type="number" name="options[<%= index %>][price]" step="0.01" 
                                           placeholder="加價" value="<%= option.price %>">
                                    <button type="button" class="remove-option-btn" onclick="removeOption(this)">刪除</button>
                                </div>
                            <% }); %>
                        <% } %>
                    </div>
                    
                    <button type="button" class="add-option-btn" onclick="addOption()">➕ 新增選項</button>
                </div>
            </div>
            
            <div class="action-buttons">
                <button type="submit" class="save-btn">💾 儲存變更</button>
                <a href="/admin/products" class="cancel-btn">❌ 取消</a>
            </div>
        </form>
    </div>
    
    <script>
        let optionIndex = <%= (product.options && product.options.length) || 0 %>;
        
        // 圖片預覽
        document.getElementById('productImage').addEventListener('change', function(e) {
            const file = e.target.files[0];
            if (file) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    const preview = document.getElementById('imagePreview');
                    preview.src = e.target.result;
                    preview.style.display = 'block';
                };
                reader.readAsDataURL(file);
            }
        });
        
        // 新增選項
        function addOption() {
            const optionsList = document.getElementById('optionsList');
            const optionItem = document.createElement('div');
            optionItem.className = 'option-item';
            optionItem.innerHTML = `
                <input type="text" name="options[${optionIndex}][name]" 
                       placeholder="選項名稱" required>
                <input type="number" name="options[${optionIndex}][price]" step="0.01" 
                       placeholder="加價">
                <button type="button" class="remove-option-btn" onclick="removeOption(this)">刪除</button>
            `;
            optionsList.appendChild(optionItem);
            optionIndex++;
        }
        
        // 移除選項
        function removeOption(button) {
            button.parentElement.remove();
        }
        
        // 計價商品切換
        document.getElementById('isPricedItem').addEventListener('change', function() {
            const priceInput = document.querySelector('input[name="price"]');
            if (this.checked) {
                priceInput.value = '';
                priceInput.disabled = true;
                priceInput.placeholder = '計價商品無固定價格';
            } else {
                priceInput.disabled = false;
                priceInput.placeholder = '例：80';
            }
        });
    </script>
</body>
</html>