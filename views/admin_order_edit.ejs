<!DOCTYPE html>
<html lang="zh-Hant">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>訂單詳情 #<%= order.id %> - 誠意鮮蘉管理後台</title>
    <link rel="stylesheet" href="/css/admin-dashboard.css">
    <style>
        .order-container {
            padding: 20px;
            max-width: 1200px;
            margin: 0 auto;
        }
        
        .order-header {
            background: linear-gradient(135deg, #2c3e50, #3498db);
            color: white;
            padding: 30px;
            border-radius: 12px;
            margin-bottom: 30px;
            position: relative;
        }
        
        .order-info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }
        
        .info-card {
            background: white;
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        
        .info-title {
            font-size: 1.1em;
            font-weight: bold;
            color: #2c3e50;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .info-item {
            margin-bottom: 10px;
            display: flex;
            justify-content: space-between;
        }
        
        .info-label {
            color: #7f8c8d;
            flex: 1;
        }
        
        .info-value {
            flex: 2;
            font-weight: 500;
        }
        
        .status-badge {
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 0.9em;
            font-weight: bold;
            color: white;
            position: absolute;
            top: 20px;
            right: 20px;
        }
        
        .status-placed { background: #f39c12; }
        .status-packaging { background: #3498db; }
        .status-delivering { background: #9b59b6; }
        .status-delivered { background: #27ae60; }
        
        .items-table {
            background: white;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            margin-bottom: 30px;
        }
        
        .table-header {
            background: linear-gradient(135deg, #34495e, #2c3e50);
            color: white;
            padding: 15px 20px;
            font-weight: bold;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
        }
        
        th, td {
            padding: 15px;
            text-align: left;
            border-bottom: 1px solid #ecf0f1;
        }
        
        th {
            background: #34495e;
            color: white;
            font-weight: 600;
        }
        
        .item-name {
            font-weight: bold;
            color: #2c3e50;
        }
        
        .item-type {
            padding: 3px 8px;
            border-radius: 12px;
            font-size: 0.8em;
            color: white;
        }
        
        .type-fixed { background: #27ae60; }
        .type-priced { background: #e67e22; }
        
        .price-input {
            width: 100px;
            padding: 5px;
            border: 1px solid #ddd;
            border-radius: 4px;
        }
        
        .weight-input {
            width: 80px;
            padding: 5px;
            border: 1px solid #ddd;
            border-radius: 4px;
        }
        
        .total-section {
            background: white;
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px;
        }
        
        .total-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
            padding: 5px 0;
        }
        
        .total-row.final {
            border-top: 2px solid #3498db;
            margin-top: 15px;
            padding-top: 15px;
            font-size: 1.2em;
            font-weight: bold;
            color: #2c3e50;
        }
        
        .action-buttons {
            display: flex;
            gap: 15px;
            margin-bottom: 20px;
        }
        
        .btn {
            padding: 12px 25px;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            font-weight: bold;
            text-decoration: none;
            transition: all 0.3s ease;
        }
        
        .btn-primary {
            background: #3498db;
            color: white;
        }
        
        .btn-primary:hover {
            background: #2980b9;
        }
        
        .btn-secondary {
            background: #95a5a6;
            color: white;
        }
        
        .btn-secondary:hover {
            background: #7f8c8d;
        }
        
        .notes-section {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 8px;
            margin-top: 20px;
        }
        
        @media (max-width: 768px) {
            .order-container {
                padding: 10px;
            }
            
            .order-info-grid {
                grid-template-columns: 1fr;
            }
            
            table {
                font-size: 0.9em;
            }
            
            th, td {
                padding: 10px 8px;
            }
            
            .action-buttons {
                flex-direction: column;
            }
        }
    </style>
</head>
<body>
    <div class="order-container">
        <div class="order-header">
            <% 
                let statusClass = '';
                let statusText = '';
                switch(order.status) {
                    case 'placed': statusClass = 'status-placed'; statusText = '已下單'; break;
                    case 'packaging': statusClass = 'status-packaging'; statusText = '打包中'; break;
                    case 'delivering': statusClass = 'status-delivering'; statusText = '配送中'; break;
                    case 'delivered': statusClass = 'status-delivered'; statusText = '已完成'; break;
                    default: statusClass = 'status-placed'; statusText = order.status;
                }
            %>
            <div class="status-badge <%= statusClass %>"><%= statusText %></div>
            <h1>📄 訂單詳情 #<%= order.id %></h1>
            <p>下單時間：<%= new Date(order.created_at).toLocaleString('zh-TW') %></p>
        </div>
        
        <!-- 客戶資訊 -->
        <div class="order-info-grid">
            <div class="info-card">
                <div class="info-title">👤 客戶資訊</div>
                <div class="info-item">
                    <span class="info-label">姓名</span>
                    <span class="info-value"><%= order.contact_name %></span>
                </div>
                <div class="info-item">
                    <span class="info-label">電話</span>
                    <span class="info-value"><%= order.contact_phone %></span>
                </div>
                <div class="info-item">
                    <span class="info-label">付款方式</span>
                    <span class="info-value">
                        <% 
                            let paymentText = '';
                            switch(order.payment_method) {
                                case 'line_pay': paymentText = 'LINE Pay'; break;
                                case 'bank_transfer': paymentText = '銀行轉帳'; break;
                                case 'cash_on_delivery': paymentText = '貨到付款'; break;
                                default: paymentText = order.payment_method || '未設定';
                            }
                        %>
                        <%= paymentText %>
                    </span>
                </div>
            </div>
            
            <div class="info-card">
                <div class="info-title">📦 配送資訊</div>
                <div class="info-item">
                    <span class="info-label">配送地址</span>
                    <span class="info-value"><%= order.address %></span>
                </div>
                <% if (order.delivery_notes) { %>
                    <div class="info-item">
                        <span class="info-label">備註</span>
                        <span class="info-value"><%= order.delivery_notes %></span>
                    </div>
                <% } %>
            </div>
        </div>
        <!-- 訂單商品明細 -->
        <div class="items-table">
            <div class="table-header">
                🛍️ 訂單商品明細 - 共 <%= order.items ? order.items.length : 0 %> 項商品
            </div>
            
        <form method="post" action="/admin/orders/<%= order.id %>">
            <table>
                <thead>
                    <tr>
                        <th>商品名稱</th>
                        <th>類型</th>
                        <th>數量</th>
                        <th>單價</th>
                        <th>小計（可編輯）</th>
                        <th>實際重量</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (order.items && order.items.length > 0) { %>
                        <% order.items.forEach(function(item) { %>
                            <tr>
                                <td class="item-name"><%= item.name %></td>
                                <td>
                                    <span class="item-type <%= item.is_priced_item ? 'type-priced' : 'type-fixed' %>">
                                        <%= item.is_priced_item ? '計價商品' : '固定價格' %>
                                    </span>
                                </td>
                                <td><strong><%= item.quantity %></strong></td>
                                <td>
                                    <% if (item.unit_price) { %>
                                        <strong style="color: #27ae60;">$<%= item.unit_price %></strong>
                                    <% } else { %>
                                        <span style="color: #95a5a6;">依重量計價</span>
                                    <% } %>
                                </td>
                                <td>
                                    <% if (item.is_priced_item) { %>
                                        <input type="number" name="lineTotal" step="0.01" value="<%= item.line_total %>" 
                                               class="price-input" required>
                                    <% } else { %>
                                        <strong style="color: #27ae60;">$<%= item.line_total %></strong>
                                        <input type="hidden" name="lineTotal" value="<%= item.line_total %>">
                                    <% } %>
                                </td>
                                <td>
                                    <% if (item.is_priced_item) { %>
                                        <input type="number" name="actualWeight" step="0.01" 
                                               value="<%= item.actual_weight || '' %>" 
                                               class="weight-input" placeholder="公斤">
                                    <% } else { %>
                                        <input type="hidden" name="actualWeight" value="">
                                        <span style="color: #95a5a6;">-</span>
                                    <% } %>
                                </td>
                            </tr>
                        <% }); %>
                    <% } else { %>
                        <tr>
                            <td colspan="6" style="text-align: center; padding: 40px; color: #95a5a6;">
                                此訂單沒有商品明細
                            </td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
            
            <!-- 費用明細 -->
            <div class="total-section">
                <div class="total-row">
                    <span>商品小計</span>
                    <span><strong>$<%= order.subtotal || (order.total - (order.delivery_fee || 50)) %></strong></span>
                </div>
                <div class="total-row">
                    <span>運費</span>
                    <span><strong>$<%= order.delivery_fee || 50 %></strong></span>
                </div>
                <div class="total-row final">
                    <span>訂單總額</span>
                    <span style="color: #27ae60;"><strong>$<%= order.total %></strong></span>
                </div>
            </div>
            
            <!-- 操作按鈕 -->
            <div class="action-buttons">
                <button type="submit" class="btn btn-primary">💾 更新訂單金額</button>
                <a href="/admin/orders" class="btn btn-secondary">← 回訂單列表</a>
            </div>
        </form>
        
        <!-- 配送備註 -->
        <% if (order.delivery_notes) { %>
            <div class="notes-section">
                <h3>📝 配送備註</h3>
                <p><%= order.delivery_notes %></p>
            </div>
        <% } %>
    </div>
</body>
</html>